<?xml?>
<config>
<!--
    amq_vhost.cfg - OpenAMQ virtual host configuration file

    This file provides virtual host configuration.
 -->

<vhost>
    <name value = "/" />
    <text value = "Default virtual-host for this server"/>
</vhost>

<workdir>
    <!-- This is where the Berkeley DB backing store is held      -->
    <data  value = "data"  />
    <!-- This is where partially uploaded large messages are held -->
    <spool value = "spool" />
    <!-- This is where fully-uploaded large messages are held     -->
    <store value = "store" />
</workdir>

<queue>
    <name value = "test-small" />
    <name value = "test-large" />
</queue>

</config>
