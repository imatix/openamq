if [ -z "$1" ]; then
    for file in `egrep -l gdl *.txt`; do
        base=`basename $file .txt`
        mw $base
    done
else
    echo Generating $1...
    mkgdl $1.txt
    gsl3 -quiet:1 -q -gdl:$1 -tpl:wikidot gurudoc
fi

