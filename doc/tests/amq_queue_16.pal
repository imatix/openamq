<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "Purge a queue which is non existant"
    >
    This test sets up a session, and tries to purge a non existant queue.
    This should yield a channel error.

    
    <session error_handler = "recover">
        <queue_purge queue = "nonexistant" />
    </session>
</pal>


