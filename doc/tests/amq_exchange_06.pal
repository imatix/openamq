<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "Declare exchange with access ticket and test if exchange is bindable with no ticket"
    >
    This test sets up a session and declares a exchange with an access ticket.
    Binding this exchange should require the access ticket. Here we test if
    the exchange is bindable with no ticket argument, binding should fail.
    
    <include filename = "amq_test_base.pal" />
    <session>
        <queue_declare queue = "myqueue" />
        <exchange_declare exchange = "myexchange" type = "fanout" ticket = "12345" />
        <queue_bind queue = "myqueue" exchange = "myexchange" />
        <abort>Could not make test fail, no error on bind w/o ticket</abort>
    </session>
</pal>


