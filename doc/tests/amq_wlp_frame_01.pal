<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "the server MUST handle incorrect frame types correctly" 
    >
    <!--
    - connect
    - send valid header
    - read from socket and discard data
    - send %xFF to socket
    - assert that server closes socket
    -->
    <include filename = "amq_test_base.pal" />
    <session>
    </session> 
</pal>

