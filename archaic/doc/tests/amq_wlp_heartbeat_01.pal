<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "the server MUST detect invalid heartbeat frame" 
    >
    <!--
    - connect and open a session
    - send a heartbeat frame with channel number GT zero
    - check that server replies with Connection.Close and reply code
      501 (frame error).
    -->
    <abort>not implemented</abort>
    <include filename = "amq_test_base.pal" />
    <session>
    </session> 
</pal>


