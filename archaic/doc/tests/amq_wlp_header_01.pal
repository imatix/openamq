<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "the server MUST accept the AMQP protocol" 
    >
    <!--
    - connect to server socket
    - send AMQP%d1.1.9.1
    - assert that server responds with Connection.Start
    -->
    <abort>not implemented</abort>
    <include filename = "amq_test_base.pal" />
    <session>
    </session> 
</pal>

