<?xml?>
<pal
    script = "amq_pal_gen"
    desc   = "the server MUST validate the content structure" 
    >
    <!--
    - connect and open a session
    - format a content with an invalid structure (too large weight)
    - send the content
    - check that server replies with Connection.Close and reply code
      501 (frame error).
    -->
    <abort>not implemented</abort>
    <include filename = "amq_test_base.pal" />
    <session>
    </session> 
</pal>

