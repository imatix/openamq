<?xml?>
<pal
    name = "amq_queue_declare-durable"
    script = "amq_pal_gen"
    target = "stdc"
    >
    <session scope="default">
        <queue_declare queue="queue-durable_1" durable="1" scope="default" />
        <!-- 
        FIXME: Future testcase: check if queue persists over server restart
        -->
    </session>
</pal>
